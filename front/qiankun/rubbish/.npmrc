# pnpm 配置文件
# 启用依赖提升，解决模块找不到的问题
hoist=true
hoist-pattern[]=*

# 设置 shamefully-hoist 为 true，确保所有依赖都能被正确提升
shamefully-hoist=true

# 启用 public-hoist-pattern，允许公共依赖被提升到根目录
public-hoist-pattern[]=*

# 设置 node-linker 为 hoisted，使用提升的链接方式
node-linker=hoisted

# 启用 auto-install-peers，自动安装 peer dependencies
auto-install-peers=true

# 设置 registry
registry=https://registry.npmjs.org/