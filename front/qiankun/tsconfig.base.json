{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@base": ["./*"],
      "@shared": ["./shared/*"],
    },
    //TypeScript智能感知在模板中是禁用的,可以使用"preserve"来启用
    "jsx": "preserve"
  },
  "exclude": ["node_modules", "dist"]
}
